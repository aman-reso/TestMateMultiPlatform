CREATE TABLE Post (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    content TEXT NOT NULL
);

insertPost:
INSERT INTO Post(title, content)
VALUES (?, ?);

getAllPosts:
SELECT * FROM Post;